import { Button, Card, StatusBadge } from '@claim-studio/ui';
import { useEffect, useState } from 'react';
import { ApiError, apiRequest } from '../api';
import { StatusFeedbackState } from '../layout/StatusFeedbackState';
import { announceWorkspacePreferences, type WorkspaceDensity, type WorkspaceFontFamily, type WorkspacePreferences, type WorkspaceTheme } from '../settings/workspace-preferences';
import { PreviewGoogleDriveSetup } from './PreviewEvidenceHub';
import type { UserRole } from './Router';

type ProviderKind = 'OPENAI' | 'ANTHROPIC' | 'GEMINI';
type CredentialScope = 'USER' | 'ORGANIZATION';
type SettingsSection = 'PERSONAL' | 'ADMIN';
interface CredentialState { configured: boolean; storage: 'ENCRYPTED_D1' | 'CLOUDFLARE_SECRET' | 'NONE'; version: number; updatedAt: string | null; fingerprint: string | null }
interface ProviderState { providerKind: ProviderKind; label: string; personal: CredentialState; organization: CredentialState }
interface SettingsPayload { personalPriority: boolean; masterKeyReady: boolean; canManageOrganization: boolean; providers: ProviderState[] }
interface WorkspacePolicy { organizationName: string; localAiMode: 'DISABLED' | 'PRIVATE_SERVER_BRIDGE'; memoryProvider: 'NONE' | 'HERMES_AGENT'; memoryApprovalMode: 'ADMIN_REVIEW' | 'DISABLED'; shortTermMemoryEnabled: boolean; longTermMemoryEnabled: boolean; version: number; updatedAt: string | null }
interface WorkspaceRuntime { localAi: string; hermes: string; memoryLearning: string; supportedLocalProviders: string[] }
interface MemoryCandidate { id: string; memoryScope: string; scopeKey: string; problemText: string; ruleText: string; tags: string[]; analyzerCode: string; confidence: number; status: 'PENDING' | 'ACTIVE' | 'REJECTED' | 'DISABLED'; version: number; createdAt: string; reviewedAt: string | null; feedbackText: string; chapterCode: string; caseNumber: string; caseTitle: string; createdByName: string }

const PROVIDER_COPY: Record<ProviderKind, { short: string; use: string; placeholder: string }> = {
  OPENAI: { short: 'ChatGPT', use: '목차 기획과 구조 설계', placeholder: 'OpenAI API Key' },
  ANTHROPIC: { short: 'Claude', use: '장문 보고서 본문 작성', placeholder: 'Anthropic API Key' },
  GEMINI: { short: 'Gemini', use: '글쓰기 도우미·문장 개선·사실 확인', placeholder: 'Google AI Studio API Key' }
};

export function PreviewSettings({ roles, onNavigate }: { roles: UserRole[]; onNavigate: (path: string) => void }): React.ReactElement {
  const isAdmin = roles.includes('admin');
  const requestedSection = new URLSearchParams(window.location.search).get('section');
  const [section, setSection] = useState<SettingsSection>(requestedSection === 'admin' && isAdmin ? 'ADMIN' : 'PERSONAL');
  const [payload, setPayload] = useState<SettingsPayload | null>(null);
  const [preferences, setPreferences] = useState<WorkspacePreferences | null>(null);
  const [workspace, setWorkspace] = useState<WorkspacePolicy | null>(null);
  const [runtime, setRuntime] = useState<WorkspaceRuntime | null>(null);
  const [memoryCandidates, setMemoryCandidates] = useState<MemoryCandidate[]>([]);
  const [keys, setKeys] = useState<Record<string, string>>({});
  const [busy, setBusy] = useState('');
  const [error, setError] = useState('');
  const [notice, setNotice] = useState('');
  const [loading, setLoading] = useState(true);

  const load = async () => {
    setLoading(true); setError('');
    try {
      const [credentials, preferenceResult] = await Promise.all([apiRequest<SettingsPayload>('/api/settings/ai-credentials'), apiRequest<{ preferences: WorkspacePreferences }>('/api/settings/preferences')]);
      setPayload(credentials); setPreferences(preferenceResult.preferences); announceWorkspacePreferences(preferenceResult.preferences);
      if (isAdmin) { const [admin, memory] = await Promise.all([apiRequest<{ settings: WorkspacePolicy; runtime: WorkspaceRuntime }>('/api/settings/admin-workspace'),apiRequest<{ candidates: MemoryCandidate[] }>('/api/admin/report-memory')]); setWorkspace(admin.settings); setRuntime(admin.runtime); setMemoryCandidates(memory.candidates); }
    } catch (reason) { setError(reason instanceof Error ? reason.message : String(reason)); }
    finally { setLoading(false); }
  };
  useEffect(() => { void load(); }, []);

  const changeSection = (next: SettingsSection) => { if (next === 'ADMIN' && !isAdmin) return; setSection(next); window.history.replaceState({}, '', next === 'ADMIN' ? '/settings?section=admin' : '/settings?section=personal'); };
  const stateFor = (provider: ProviderState, scope: CredentialScope) => scope === 'USER' ? provider.personal : provider.organization;
  const inputKey = (provider: ProviderKind, scope: CredentialScope) => `${scope}:${provider}`;
  const saveKey = async (provider: ProviderState, scope: CredentialScope) => {
    const state = stateFor(provider, scope); const key = keys[inputKey(provider.providerKind, scope)]?.trim() ?? ''; if (!key) return;
    setBusy(inputKey(provider.providerKind, scope)); setError(''); setNotice('');
    try { const next = await apiRequest<SettingsPayload>(`/api/settings/ai-credentials/${provider.providerKind}`, { method: 'PUT', body: JSON.stringify({ scope, apiKey: key, expectedVersion: state.version }) }); setPayload(next); setKeys((current) => ({ ...current, [inputKey(provider.providerKind, scope)]: '' })); setNotice(`${scope === 'USER' ? '내' : '조직 공용'} ${PROVIDER_COPY[provider.providerKind].short} 키를 암호화해 저장했습니다.`); }
    catch (reason) { setError(reason instanceof ApiError && reason.status === 409 ? '다른 화면에서 설정이 변경되었습니다. 새로고침 후 다시 저장해 주세요.' : reason instanceof Error ? reason.message : String(reason)); }
    finally { setBusy(''); }
  };
  const disableKey = async (provider: ProviderState, scope: CredentialScope) => { const state = stateFor(provider, scope); setBusy(inputKey(provider.providerKind, scope)); setError(''); setNotice(''); try { setPayload(await apiRequest<SettingsPayload>(`/api/settings/ai-credentials/${provider.providerKind}`, { method: 'DELETE', body: JSON.stringify({ scope, expectedVersion: state.version }) })); setNotice(`${PROVIDER_COPY[provider.providerKind].short} 키를 안전하게 비활성화했습니다.`); } catch (reason) { setError(reason instanceof Error ? reason.message : String(reason)); } finally { setBusy(''); } };
  const testKey = async (provider: ProviderState, scope: CredentialScope) => { setBusy(`test:${inputKey(provider.providerKind, scope)}`); setError(''); setNotice(''); try { const result = await apiRequest<{ source: string; checkedAt: string }>(`/api/settings/ai-credentials/${provider.providerKind}/test`, { method: 'POST', body: JSON.stringify({ scope }) }); setNotice(`${PROVIDER_COPY[provider.providerKind].short} 연결 확인 완료 · ${result.source} · ${new Date(result.checkedAt).toLocaleString('ko-KR')}`); } catch (reason) { setError(reason instanceof Error ? reason.message : String(reason)); } finally { setBusy(''); } };
  const savePreferences = async () => { if (!preferences) return; setBusy('preferences'); setError(''); setNotice(''); try { const result = await apiRequest<{ preferences: WorkspacePreferences }>('/api/settings/preferences', { method: 'PUT', body: JSON.stringify({ theme: preferences.theme, fontFamily: preferences.fontFamily, fontScale: preferences.fontScale, density: preferences.density, reduceMotion: preferences.reduceMotion, expectedVersion: preferences.version }) }); setPreferences(result.preferences); announceWorkspacePreferences(result.preferences); setNotice('개인 화면 설정을 D1에 저장했습니다. 다른 기기에서도 로그인하면 적용됩니다.'); } catch (reason) { setError(reason instanceof Error ? reason.message : String(reason)); } finally { setBusy(''); } };
  const saveWorkspace = async () => { if (!workspace) return; setBusy('workspace'); setError(''); setNotice(''); try { const result = await apiRequest<{ settings: WorkspacePolicy; runtime: WorkspaceRuntime }>('/api/settings/admin-workspace', { method: 'PUT', body: JSON.stringify({ organizationName: workspace.organizationName, localAiMode: workspace.localAiMode, memoryProvider: workspace.memoryProvider, memoryApprovalMode: workspace.memoryApprovalMode, shortTermMemoryEnabled: workspace.shortTermMemoryEnabled, longTermMemoryEnabled: workspace.longTermMemoryEnabled, expectedVersion: workspace.version }) }); setWorkspace(result.settings); setRuntime(result.runtime); setNotice('관리자 워크스페이스 정책을 D1에 저장했습니다.'); } catch (reason) { setError(reason instanceof Error ? reason.message : String(reason)); } finally { setBusy(''); } };
  const decideMemory = async (candidate: MemoryCandidate, action: 'APPROVE' | 'REJECT' | 'DISABLE') => { setBusy(`memory:${candidate.id}`); setError(''); setNotice(''); try { const result = await apiRequest<{ candidates: MemoryCandidate[] }>(`/api/admin/report-memory/${candidate.id}`, { method: 'PUT', body: JSON.stringify({ action, expectedVersion: candidate.version, note: action === 'APPROVE' ? '관리자 검토 후 다음 생성에 반영' : action === 'REJECT' ? '관리자 검토에서 반영 제외' : '관리자에 의해 비활성화' }) }); setMemoryCandidates(result.candidates); setNotice(action === 'APPROVE' ? 'Memory를 승인했습니다. 다음 관련 챕터 생성부터 검색·반영됩니다.' : action === 'DISABLE' ? '활성 Memory를 비활성화했습니다.' : '학습 후보를 반려했습니다.'); } catch (reason) { setError(reason instanceof ApiError && reason.status === 409 ? '다른 관리자가 먼저 처리했습니다. 설정을 다시 불러와 주세요.' : reason instanceof Error ? reason.message : String(reason)); } finally { setBusy(''); } };

  if (loading) return <StatusFeedbackState type="loading" message="개인·관리자 설정과 연결 상태를 불러오고 있습니다." />;
  if (!payload || !preferences) return <StatusFeedbackState type="error" title="설정을 불러오지 못했습니다" message={error || '잠시 후 다시 시도해 주세요.'} actionLabel="다시 시도" onAction={() => void load()} />;

  const renderCredentials = (scope: CredentialScope, title: string, detail: string) => <Card title={title} className="credential-settings-card">
    <header className="credential-settings-card__intro"><div><p>{detail}</p><small>{scope === 'USER' ? '개인키가 있으면 조직 공용키보다 먼저 사용합니다.' : '개인키가 없는 사용자에게만 공용키가 사용됩니다.'}</small></div><StatusBadge status={scope === 'USER' ? 'review' : 'approved'} /></header>
    <div className="credential-provider-grid">{payload.providers.map((provider) => { const state = stateFor(provider, scope); const field = inputKey(provider.providerKind, scope); const isBusy = busy === field || busy === `test:${field}`; return <section key={provider.providerKind} data-provider={provider.providerKind} data-configured={state.configured}><header><span>{PROVIDER_COPY[provider.providerKind].short.slice(0, 2).toUpperCase()}</span><div><h3>{provider.label}</h3><p>{PROVIDER_COPY[provider.providerKind].use}</p></div><strong>{state.configured ? '연결됨' : '키 필요'}</strong></header><div className="credential-state"><span>{state.storage === 'ENCRYPTED_D1' ? 'AES-256-GCM 암호화 저장' : state.storage === 'CLOUDFLARE_SECRET' ? 'Cloudflare 서버 Secret' : '저장된 키 없음'}</span>{state.fingerprint && <small>키 지문 {state.fingerprint}… · v{state.version}</small>}</div><label htmlFor={`${field}-key`}>{state.configured ? '새 키로 교체' : 'API Key 입력'}</label><input id={`${field}-key`} type="password" value={keys[field] ?? ''} autoComplete="new-password" spellCheck={false} placeholder={PROVIDER_COPY[provider.providerKind].placeholder} onChange={(event) => setKeys((current) => ({ ...current, [field]: event.target.value }))} />{!keys[field]?.trim() && <small className="credential-input-help">API 키를 입력하면 암호화 저장 버튼이 활성화됩니다.</small>}<div className="action-row"><Button onClick={() => void saveKey(provider, scope)} disabled={isBusy || !keys[field]?.trim()}>{isBusy ? '처리 중…' : state.configured ? '키 교체' : '암호화 저장'}</Button>{state.configured && <Button variant="secondary" onClick={() => void testKey(provider, scope)} disabled={isBusy}>연결 확인</Button>}{state.storage === 'ENCRYPTED_D1' && <Button variant="ghost" onClick={() => void disableKey(provider, scope)} disabled={isBusy}>비활성화</Button>}</div></section>; })}</div>
  </Card>;

  return <div className="content-stack preview-settings" aria-label="개인 및 관리자 설정">
    <section className="preview-settings-hero"><div><span>WORKSPACE CONTROL CENTER</span><h2>톱니바퀴 하나에서<br />개인 설정과 관리자 설정을 구분합니다.</h2><p>내 화면·내 AI는 개인 설정에서, 회사 Drive·공용 AI·Hermes 정책은 관리자 설정에서 관리합니다.</p></div><div><strong>{payload.masterKeyReady ? '암호화 저장 준비됨' : '서버 암호화키 필요'}</strong><small>키 원문은 브라우저와 API 응답에 다시 표시하지 않습니다.</small></div></section>
    <nav className="settings-section-tabs" aria-label="설정 종류"><button type="button" className={section === 'PERSONAL' ? 'is-active' : ''} aria-current={section === 'PERSONAL' ? 'page' : undefined} onClick={() => changeSection('PERSONAL')}><span>PERSONAL</span><strong>개인 설정</strong><small>화면·글꼴·개인 AI</small></button>{isAdmin && <button type="button" className={section === 'ADMIN' ? 'is-active' : ''} aria-current={section === 'ADMIN' ? 'page' : undefined} onClick={() => changeSection('ADMIN')}><span>ADMIN ONLY</span><strong>관리자 설정</strong><small>회사 Drive·공용 AI·Memory</small></button>}</nav>
    <section className="settings-access-strip" aria-label="현재 계정 설정 권한"><div><span>현재 로그인 역할</span><strong>{roles.map((role) => role.toUpperCase()).join(' · ') || 'USER'}</strong></div><p>{section === 'PERSONAL' ? '현재 화면은 나에게만 적용됩니다.' : '조직 전체에 적용되는 관리자 전용 화면입니다.'} {!isAdmin && '관리자 설정은 Admin 계정으로 로그인해야 접근할 수 있습니다.'}</p></section>
    {section === 'PERSONAL' && <><Card title="내 화면 맞춤 설정"><div className="personal-preference-grid"><label>화면 모드<select value={preferences.theme} onChange={(event) => setPreferences({ ...preferences, theme: event.target.value as WorkspaceTheme })}><option value="LIGHT">라이트 모드</option><option value="DARK">다크 모드</option></select></label><label>글꼴<select value={preferences.fontFamily} onChange={(event) => setPreferences({ ...preferences, fontFamily: event.target.value as WorkspaceFontFamily })}><option value="PRETENDARD">Pretendard</option><option value="NOTO_SANS_KR">Noto Sans KR</option><option value="SYSTEM">시스템 기본 글꼴</option></select></label><label>글자 크기 <b>{preferences.fontScale}%</b><input type="range" min="90" max="130" step="5" value={preferences.fontScale} onChange={(event) => setPreferences({ ...preferences, fontScale: Number(event.target.value) })} /></label><label>화면 간격<select value={preferences.density} onChange={(event) => setPreferences({ ...preferences, density: event.target.value as WorkspaceDensity })}><option value="COMFORTABLE">편안하게</option><option value="COMPACT">조밀하게</option></select></label><label className="settings-check"><input type="checkbox" checked={preferences.reduceMotion} onChange={(event) => setPreferences({ ...preferences, reduceMotion: event.target.checked })} />화면 움직임 최소화</label><div className="settings-preference-preview"><span>미리보기</span><strong>읽기 쉬운 클레임 보고서</strong><small>글꼴과 크기·간격을 확인하세요.</small></div></div><div className="action-row"><Button onClick={() => void savePreferences()} disabled={busy === 'preferences'}>{busy === 'preferences' ? '저장 중…' : '개인 화면 설정 저장'}</Button></div></Card>{renderCredentials('USER', '내 AI 설정', '내 계정에서만 사용하는 개인 API 키입니다. 관리자도 원문을 볼 수 없습니다.')}<Card title="로컬 AI 연결 가이드"><div className="local-ai-guide"><div><span>01</span><strong>로컬 모델 실행</strong><p>Ollama, LM Studio 또는 OpenAI Compatible 서버에서 모델을 실행합니다.</p><code>http://localhost:11434</code></div><div><span>02</span><strong>회사 서버 Bridge</strong><p>Cloudflare는 개인 PC의 localhost에 직접 접근할 수 없습니다. 향후 공유 서버에 HTTPS 전용 Bridge를 설치합니다.</p><code>HTTPS · VPN · 접근제어 필수</code></div><div><span>03</span><strong>관리자 활성화</strong><p>관리자 설정에서 PRIVATE_SERVER_BRIDGE를 승인하고 업무별 모델을 선택합니다.</p><code>현재 상태 · 직접 호출 비활성</code></div></div></Card></>}
    {section === 'ADMIN' && isAdmin && workspace && <><Card title="조직·로컬 AI·Hermes Memory 정책"><div className="workspace-policy-grid"><label>조직 표시명<input value={workspace.organizationName} maxLength={80} onChange={(event) => setWorkspace({ ...workspace, organizationName: event.target.value })} /></label><label>로컬 AI 정책<select value={workspace.localAiMode} onChange={(event) => setWorkspace({ ...workspace, localAiMode: event.target.value as WorkspacePolicy['localAiMode'] })}><option value="DISABLED">비활성</option><option value="PRIVATE_SERVER_BRIDGE">회사 전용 Server Bridge 준비</option></select></label><label>Memory Agent<select value={workspace.memoryProvider} onChange={(event) => setWorkspace({ ...workspace, memoryProvider: event.target.value as WorkspacePolicy['memoryProvider'], shortTermMemoryEnabled: false, longTermMemoryEnabled: false })}><option value="NONE">연결 안 함</option><option value="HERMES_AGENT">Hermes Agent 호환 계층</option></select></label><label>학습 반영 방식<select value={workspace.memoryApprovalMode} onChange={(event) => setWorkspace({ ...workspace, memoryApprovalMode: event.target.value as WorkspacePolicy['memoryApprovalMode'] })}><option value="ADMIN_REVIEW">관리자 승인 후 반영</option><option value="DISABLED">학습 비활성</option></select></label><label className="settings-check"><input type="checkbox" disabled={workspace.memoryProvider !== 'HERMES_AGENT'} checked={workspace.shortTermMemoryEnabled} onChange={(event) => setWorkspace({ ...workspace, shortTermMemoryEnabled: event.target.checked })} />프로젝트 단기기억 정책</label><label className="settings-check"><input type="checkbox" disabled={workspace.memoryProvider !== 'HERMES_AGENT'} checked={workspace.longTermMemoryEnabled} onChange={(event) => setWorkspace({ ...workspace, longTermMemoryEnabled: event.target.checked })} />회사 장기기억 후보 정책</label></div><div className="settings-runtime-status"><div><span>LOCAL AI</span><strong>{runtime?.localAi ?? 'DISABLED'}</strong></div><div><span>HERMES LAYER</span><strong>{runtime?.hermes ?? 'DISABLED'}</strong></div><div><span>LEARNING LOOP</span><strong>{runtime?.memoryLearning ?? 'FOUNDATION_ONLY'}</strong></div></div><p className="settings-honest-note"><strong>실제 학습 경계</strong> 보고서 단계에서 AI 초안과 저장된 사람 수정본을 비교해 구조화 후보를 만들고, 아래 관리자 승인 후에만 다음 관련 챕터 프롬프트에 반영합니다. 외부 Hermes 실행기는 아직 미연결이며 현재는 같은 인터페이스의 결정론적 분석기를 사용합니다.</p><div className="action-row"><Button onClick={() => void saveWorkspace()} disabled={busy === 'workspace'}>{busy === 'workspace' ? '저장 중…' : '관리자 정책 저장'}</Button></div></Card><Card title={`AI Memory 관리 · ${memoryCandidates.filter((item) => item.status === 'PENDING').length}개 승인 대기`}><div className="memory-candidate-list">{memoryCandidates.length ? memoryCandidates.map((candidate) => <article key={candidate.id} data-memory-status={candidate.status}><header><div><span>{candidate.memoryScope} · {candidate.scopeKey}</span><strong>{candidate.caseNumber} · {candidate.chapterCode}</strong><small>{candidate.caseTitle} · {candidate.createdByName} · 신뢰도 {candidate.confidence}%</small></div><em>{candidate.status}</em></header><p><b>사용자 피드백</b> {candidate.feedbackText}</p><p><b>구조화 규칙</b> {candidate.ruleText}</p><div className="action-row">{candidate.status === 'PENDING' && <><Button onClick={() => void decideMemory(candidate,'APPROVE')} disabled={busy === `memory:${candidate.id}`}>승인·다음 생성 반영</Button><Button variant="secondary" onClick={() => void decideMemory(candidate,'REJECT')} disabled={busy === `memory:${candidate.id}`}>반려</Button></>}{candidate.status === 'ACTIVE' && <Button variant="secondary" onClick={() => void decideMemory(candidate,'DISABLE')} disabled={busy === `memory:${candidate.id}`}>비활성화</Button>}</div></article>) : <p className="empty-box">아직 학습 후보가 없습니다. 보고서 4단계에서 사람 수정 후 “학습 후보 등록”을 사용하면 여기에 표시됩니다.</p>}</div></Card>{renderCredentials('ORGANIZATION', '조직 공용 AI 설정', '개인 키가 없는 직원에게 적용되는 회사 공용 암호화 키입니다.')}<Card title="관리자 기능"><div className="settings-admin-links"><button type="button" onClick={() => onNavigate('/ai-config')}><strong>챕터 프롬프트·모델</strong><small>목차·본문·사실확인 역할별 모델과 관리자 프롬프트</small></button><button type="button" onClick={() => onNavigate('/integrations/google')}><strong>Google Drive 상세 설정</strong><small>회사 계정 OAuth 연결·계정 교체·연결 해제</small></button><button type="button" onClick={() => onNavigate('/users')}><strong>사용자·권한</strong><small>회원 계정과 역할 관리</small></button></div></Card><PreviewGoogleDriveSetup onNavigate={onNavigate} /></>}
    {notice && <p className="notice-box" role="status">{notice}</p>}{error && <p className="error-box" role="alert">{error}</p>}
  </div>;
}
